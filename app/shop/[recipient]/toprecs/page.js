"use client"
import React from "react"
import { useDispatch } from "react-redux"
import { getSingleCategory } from "../../../../store/shopSlice"

const TopRecsPage = () => {
 
    // return (

        // Should top recs just be a category?? could fit in under [category] and then 
        // have some selective behavior to pull in data differently
        // Besides the data, everything else about the page is the same so I don't see a point in having it seperate
       
    // )
}

export default TopRecsPage